import { Departmentfe002angPage } from './app.po';

describe('departmentfe002ang App', function() {
  let page: Departmentfe002angPage;

  beforeEach(() => {
    page = new Departmentfe002angPage();
  });

  it('should display message saying app works', () => {
    page.navigateTo();
    expect(page.getParagraphText()).toEqual('app works!');
  });
});
